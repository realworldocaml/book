The demos in this subdirectory are built using `ocamlbuild`.
This method is considered obsolete; `dune` is preferred.

The file `myocamlbuild.ml` contains `ocamlbuild` rules that
deal with the `.messages` file.
