File "gromit.mly", line 33, characters 7-10:
Warning: the token EOF is unused.
Grammar has 13 nonterminal symbols, among which 1 start symbols.
Grammar has 23 terminal symbols.
Grammar has 60 productions.
nullable(variance) = false
nullable(signature) = false
nullable(next_label_or_token) = false
nullable(lattice_list) = false
nullable(lattice_component_list) = false
nullable(lattice) = false
nullable(label_or_token_list0) = true
nullable(label_or_token_list) = false
nullable(label_comma_list) = false
nullable(first_label_or_token) = false
nullable(constructor_list) = true
nullable(component) = false
nullable(arity) = true
first(variance) = COVARIANT CONTRAVARIANT
first(signature) = error SIGNATURE
first(next_label_or_token) = error QUOTE LPAREN LIDENT
first(lattice_list) = error LATTICE
first(lattice_component_list) = error LET COVARIANT CONTRAVARIANT CONSTRUCTOR
first(lattice) = LATTICE
first(label_or_token_list0) = error QUOTE LPAREN LIDENT
first(label_or_token_list) = error QUOTE LPAREN LIDENT
first(label_comma_list) = error LIDENT
first(first_label_or_token) = error QUOTE LPAREN LIDENT
first(constructor_list) = error UIDENT
first(component) = LET COVARIANT CONTRAVARIANT CONSTRUCTOR
first(arity) = error LBRACE
minimal(variance) = (* 1 *) COVARIANT
minimal(signature) = (* 1 *) error
minimal(next_label_or_token) = (* 1 *) LIDENT
minimal(lattice_list) = (* 1 *) error
minimal(lattice_component_list) = (* 1 *) error
minimal(lattice) = (* 2 *) LATTICE error
minimal(label_or_token_list0) = (* 0 *) 
minimal(label_or_token_list) = (* 1 *) LIDENT
minimal(label_comma_list) = (* 1 *) LIDENT
minimal(first_label_or_token) = (* 1 *) LIDENT
minimal(constructor_list) = (* 0 *) 
minimal(component) = (* 2 *) CONSTRUCTOR error
minimal(arity) = (* 0 *) 
follow(variance) = error UIDENT LESS
follow(signature) = #
follow(next_label_or_token) = error QUOTE LPAREN LIDENT LBRACK
follow(lattice_list) = END
follow(lattice_component_list) = END
follow(lattice) = error LATTICE END
follow(label_or_token_list0) = error LBRACK
follow(label_or_token_list) = error LBRACK
follow(label_comma_list) = RBRACE
follow(first_label_or_token) = error QUOTE LPAREN LIDENT LBRACK
follow(constructor_list) = #
follow(component) = error LET END COVARIANT CONTRAVARIANT CONSTRUCTOR
follow(arity) = error PRINTED
Built an LR(0) automaton with 96 states.
The grammar is not SLR(1) -- 3 states have a conflict.
The construction mode is pager.
Built an LR(1) automaton with 96 states.
Warning: 3 states have shift/reduce conflicts.
Warning: 3 shift/reduce conflicts were arbitrarily resolved.
Warning: 2 states have an end-of-stream conflict.
File "gromit.mly", line 210, characters 17-17:
Warning: production constructor_list -> is never reduced.
Warning: in total, 1 production is never reduced.
55 out of 96 states have a default reduction.
10 out of 96 states are represented.
0 out of 39 symbols keep track of their start position.
0 out of 39 symbols keep track of their end position.
52 out of 61 productions exploit shiftreduce optimization.
7 out of 96 states can peek at an error.
The generated parser cannot raise Error.
271 functions before inlining, 35 functions after inlining.
