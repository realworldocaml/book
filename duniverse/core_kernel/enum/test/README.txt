With [Enum], we are currently experimenting with an idiom by which
we have merged [doc] and [test] into a single point of entry, so
that not only all the code is tested, but also all the code is
documented.  See [doc/README.mlt].

Do not add test here, rather add test+doc in doc/.
