class ['a] stack init = object
    val mutable v : 'a list = init

    method pop =
      match v with
        hd :: tl ->
          v <- tl;
          Some hd
      | [] -> None

    method push hd =
      v <- hd :: v
end ;;
#part 1
class ['a] stack init = object
      val mutable v = init
    
      method pop = 
        match v with
          hd :: tl -> 
            v <- tl;
            Some hd
        | [] -> None

      method push hd = 
        v <- hd :: v
  end ;;
