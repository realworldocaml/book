(jbuild_version 1)

(rule (
  (targets (classes.html))
  (deps    (classes.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (command-line-parsing.html))
  (deps    (command-line-parsing.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (compiler-backend.html))
  (deps    (compiler-backend.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (compiler-frontend.html))
  (deps    (compiler-frontend.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (concurrent-programming.html))
  (deps    (concurrent-programming.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (data-serialization.html))
  (deps    (data-serialization.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (error-handling.html))
  (deps    (error-handling.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (files-modules-and-programs.html))
  (deps    (files-modules-and-programs.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (first-class-modules.html))
  (deps    (first-class-modules.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (foreign-function-interface.html))
  (deps    (foreign-function-interface.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (functors.html))
  (deps    (functors.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (garbage-collector.html))
  (deps    (garbage-collector.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (guided-tour.html))
  (deps    (guided-tour.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (imperative-programming.html))
  (deps    (imperative-programming.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (json.html))
  (deps    (json.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (lists-and-patterns.html))
  (deps    (lists-and-patterns.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (maps-and-hashtables.html))
  (deps    (maps-and-hashtables.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (objects.html))
  (deps    (objects.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (parsing-with-ocamllex-and-menhir.html))
  (deps    (parsing-with-ocamllex-and-menhir.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (prologue.html))
  (deps    (prologue.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (records.html))
  (deps    (records.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (runtime-memory-layout.html))
  (deps    (runtime-memory-layout.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (variables-and-functions.html))
  (deps    (variables-and-functions.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

(rule (
  (targets (variants.html))
  (deps    (variants.md))
  (action (
    run
    pandoc
    --section-divs
    -f
    markdown-smart-auto_identifiers
    -t
    html5
    ${<}
    -o
    ${@}))))

